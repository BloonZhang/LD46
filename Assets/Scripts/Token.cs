using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Token : MonoBehaviour
{
    // Enumuration for token type
    public enum TokenType {WATER, LIGHT, NUTRIENT, MONEY, LEGALREP, FOOD, HUNGER};

    // public variables
    public TokenType myTokenType;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
